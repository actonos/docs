---
id: mcp-integration
title: Model Context Protocol (MCP) Server Integration
sidebar_label: MCP Server Integration
sidebar_position: 5
---

# Model Context Protocol (MCP) Server Integration

ActonOS acts as a full-featured **MCP Host**, allowing agents to seamlessly discover and execute tools exposed by any standard [Model Context Protocol](https://modelcontextprotocol.io/) server.

---

## 1. Supported MCP Transports

- **`stdio`**: The ActonOS daemon spawns the MCP server as a managed sub-process and communicates over standard input/output JSON-RPC.
- **`http` / `sse`**: Connects to an existing remote MCP server running over HTTP and Server-Sent Events.

---

## 2. Adding an MCP Server

Register a server from the Tools page or with `POST /api/tools/mcp`. Configurations are stored in the ActonOS database (not a JSON file on disk). Connecting or disconnecting an MCP server may return `202 Accepted` until an operator approves the change.

```json
{
  "id": "github-mcp",
  "name": "GitHub MCP",
  "transport": "stdio",
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-github"],
  "enabled": true
}
```

For workspace documents, prefer the built-in `native_workspace_*` tools. Those keep original filenames. Do not point a filesystem MCP server at `/data/workspace` — that folder stores internal IDs, not the names you see on the Workspace page.

---

## 3. Tool Collision Handling & Sandboxing

- **Name Spacing**: ActonOS automatically namespaces tools to prevent naming collisions (e.g., `mcp_filesystem_read_file`).
- **Atomic Rollback**: If a newly registered server declares a conflicting tool signature that breaks an existing manifest, ActonOS rolls back registration and alerts the operator.
